const nomes = ['ana Julia', 'Caio vinicius', 'BIA silva'];

const nomesPadronizados = nomes.map((nome) => // Ele consegue ser últil para reescrever um array 
    nome.toUpperCase());

console.log(nomesPadronizados);